<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="com.news.newsfeed.ui.home.HomeFragment">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/viewpager_headlines"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/menu_item_margin">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tablayout_headlines"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            app:tabBackground="@drawable/tab_background"
            app:tabMode="scrollable"
            app:tabPaddingStart="@dimen/menu_item_padding"
            app:tabPaddingEnd="@dimen/menu_item_padding"
            app:tabTextColor="@color/menu_item"
            app:tabSelectedTextColor="@color/white"
            app:tabIndicatorColor="@android:color/transparent"
            app:tabRippleColor="@android:color/transparent"
            app:tabTextAppearance="@style/AppTheme.Headlines.TabTextAppearance"
            />

    </androidx.viewpager.widget.ViewPager>
</androidx.coordinatorlayout.widget.CoordinatorLayout>